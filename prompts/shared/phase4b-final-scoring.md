---
description: "Legacy compatibility boundary; external precedent never re-scores findings"
---

# Phase 4b Post-Precedent Boundary (Driver-Only)

This compatibility prompt must not launch an LLM scoring pass. Preserve
`confidence_scores.md` byte-for-byte. The deterministic driver extracts the
bounded proposal block from `rag_validation.md`, reconciles it with independent
typed mechanism/precondition facts, and writes
`precedent_evidence_authority.json` plus investigation-only `precedent_context.md`
and eligible-only `precedent_report_context.md`.

Read `~/.claude/rules/precedent-evidence-policy.md`. Generic methodology
literature supplies context only. Exact precedent requires the same mechanism
class and matching preconditions and may affect investigation priority/report
context only. It cannot change confidence, verdict, severity, proof, or depth.
Research/reconciliation failure becomes visible `UNSCORED` debt, never a
numeric floor.
