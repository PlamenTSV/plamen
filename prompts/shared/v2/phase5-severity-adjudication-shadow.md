# Phase 5: Direction-Neutral Severity Adjudication Shadow

> **Loaded by**: structural prompt reachability checks only.
> **Execution model**: DRIVER-PLANNED PROVIDER WORKERS. The driver constructs
> immutable per-shard inputs, binds the exact methodology and source roots,
> sends the dynamic prompt through provider-owned standard input, observes the
> child process, and reconciles typed execution and publication receipts.
> The generic phase dispatcher never sends this file to a model and never
> decides completion from prose or artifact presence.
>
> **Authority**: SHADOW ONLY. This phase may detect unsupported tier changes,
> retain an upstream severity when adjudication is unresolved, and expose
> visible review debt. It cannot mutate the authoritative report severity or
> certify its own output. Benchmark-authorized cutover is a separate program
> decision.
>
> **Failure mode**: COMPLETED_WITH_DEBT, never HALT. Missing inputs, provider
> failures, containment violations, ambiguous execution chains, or incomplete
> reconciliation remain explicit unresolved items.

## No generic model instructions

The live driver supplies each independent adjudicator only its assigned,
digest-bound candidate packet and required methodology. If this placeholder is
reached through generic model dispatch, stop without writing any audit artifact.
