# Exact top-level CI resolver manifest. This file and every recursively included
# requirements/constraints file are digest-bound inputs to requirements-ci.lock.
# CI consumes only the regenerated, byte-equal hash lock; ``plamen install``
# does not read this test-only manifest.
-c requirements-ci.constraints
-r requirements.txt
pytest==9.1.1
pytest-xdist==3.8.0  # `-n auto` fast-lane parallelism; already installed, pinned here
